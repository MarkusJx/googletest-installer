name: test

on:
  push

jobs:
  build:
    runs-on: ${{ matrix.os }}
    
    strategy:
      matrix:
        os: [macOS-latest, ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v2
      - name: Install googletest
        uses: ./
        id: install_gtest
      - run: sudo ldconfig -v | grep gtest
        if: matrix.os != 'windows-latest'
